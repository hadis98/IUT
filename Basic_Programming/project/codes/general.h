#ifndef GENERAL_H
#define GENERAL_H
#include "headers.h"

void setcolor(int);
void Cyan();
void Boldcyan();
void boldred();
bool is_strings_equal(char[], const char[]);
bool is_session_timeout(char[]);

#endif