#include <stdio.h>
int main()
{
	 unsigned long long int a,b,c,d,e,f;
	scanf("%llu%llu%llu%llu%llu%llu",&a,&b,&c,&d,&e,&f);
	if((a*b*c)>=(d*e*f)||a*b>=d*e||a*b>=d*f||a*b>=f*e)

	printf("zende mimuni");

	 else
	 
	printf("dari mimiri");
	
	
 return 0;
	
}
